Get resources based no [[label]]
	[[kubectl]] get <resource> --selectors <key=value> 
Filter resource based on multiple label
	[[kubectl]] get <resource> --selectors <key=value>, <key=value>,<key=value>

